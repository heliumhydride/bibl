french(OST), english (KJV,NKJV,...), russian translations in .txt/.json format

store user parameters to $HOME/.config/bibl.cfg, read file, store in simple KEY="VAL" format
install bibl executable to /usr(/local)/bin
install bibles as /usr(/local)/share/bibl/[language].txt/json
